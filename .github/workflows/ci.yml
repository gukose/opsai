name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate:
    name: Validate
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            sdk/typescript/package-lock.json
            mobile/package-lock.json

      - name: Verify OpenAPI contract
        run: ./gradlew :backend:verifyOpenApiContract

      - name: Check OpenAPI compatibility
        run: ./gradlew :backend:checkOpenApiCompatibility

      - name: Install TypeScript SDK dependencies
        working-directory: sdk/typescript
        run: npm ci

      - name: Verify generated TypeScript SDK
        run: ./gradlew verifyTypeScriptApiClient

      - name: Build TypeScript SDK
        run: ./gradlew buildTypeScriptApiClient

      - name: Test TypeScript SDK
        run: ./gradlew testTypeScriptApiClient

      - name: Verify TypeScript SDK version
        run: ./gradlew verifyTypeScriptApiClientVersion

      - name: Verify TypeScript SDK export manifest
        run: ./gradlew verifyTypeScriptApiClientExportManifest

      - name: Verify TypeScript SDK release notes
        run: ./gradlew verifyTypeScriptApiClientReleaseNotes

      - name: Pack TypeScript SDK locally
        run: ./gradlew packTypeScriptApiClient

      - name: Verify packed TypeScript SDK
        run: ./gradlew verifyPackedTypeScriptApiClient

      - name: Install mobile dependencies
        working-directory: mobile
        run: npm ci

      - name: Type-check mobile
        working-directory: mobile
        run: npx tsc --noEmit

      - name: Run mobile tests
        working-directory: mobile
        run: npm test

      - name: Run backend and UniMock tests
        run: ./gradlew :backend:test :unimock:test

      - name: Upload backend test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-reports
          path: |
            backend/build/test-results/test/
            backend/build/reports/tests/test/
          if-no-files-found: warn

      - name: Build backend Docker image
        run: docker build -f docker/backend.Dockerfile -t hotel-opai-backend:ci .

      - name: Build UniMock Docker image
        run: docker build -f docker/unimock.Dockerfile -t hotel-opai-unimock:ci .

      - name: Start smoke stack
        run: docker compose -f docker/docker-compose.smoke.yml up -d postgres unimock backend

      - name: Run smoke tests
        run: scripts/smoke/api-smoke.sh

      - name: Stop smoke stack
        if: always()
        run: docker compose -f docker/docker-compose.smoke.yml down -v
